"use strict";

let names =[]

names.push("vincent");

names.push("arthur");

names.push("paul");


for(let i = 0;i<names.length;i++){
    // names[i]=names[i] + " " + "va à la pêche"
    names[i]+=" va à la pêche"
}
console.log(names);

